# 2026-09-20 连续两次 Web GPT 对话失败

17:46–17:57（北京时间）的两个回合各尝试四次。失败快照与 daemon 日志一致：

| 回合 | DOM 观测超时 | 工具后无最终答案 | 网页 Something went wrong |
| --- | ---: | ---: | ---: |
| 9212d81f6ea8 | 2 | 1 | 1 |
| 34e2ce40be80 | 2 | 0 | 2 |

四次 DOM 超时均为单次 5 秒探针失败。至少三份可读失败快照仍有停止生成按钮；另一次连诊断状态读取也超时。因此单次探针失败不能证明生成失败。

## 已复现并修复的本地问题

- 托管 Chrome 没有 Launcher 的页面重绑回调。提交确认、等待新回复和回复节点重绑路径原先会直接抛出观测超时。现在允许两次同页重读；不重新发送、不切模型、不刷新页面。连续失败仍报错，用户取消和调用方截止时间继续有效。
- 工具后的空答案看门狗只看最终答案和 MCP 活动，忽略后续思考文字的变化。现在语义文本变化重置空答案窗口；仅有不变的旋转图标不会重置。原失败回合的助手节点从 506 字增至 691 字，但历史快照不足以判定失败前最后 60 秒是否持续推进，因此不能声称已证明这一轮一定属于误杀。

## 上游错误的证据边界

三次网页错误均有明确终态提示。记录中的流已 `stream_handoff`，后续服务端错误载荷没有被捕获。不能据此断言限流、模型容量、网络中断或具体服务端故障。保留现有有界回合重试，不增加盲目重发次数，也不吞掉终态错误。

## 验证与部署

- 新增回归测试先运行：4 失败；修复后：4 通过。
- 相关测试：135 通过；类型检查通过。
- 全量测试：1003 通过、1 跳过、0 失败。最后补齐节点重绑分支后再次运行相关 135 条和类型检查，均通过。
- macOS 安装包完成签名校验；旧 App 与 runtime 已本地归档。
- 生产后端 `/healthz` 与新 App、已安装 runtime 的 bundleId 一致：`d59e0e562746684f7c203e80c51fb6bfd7f00ec7a35bdcd86eddb12e9588783c`。
- 真实 `chatgpt-web/light` 回合完成；`chatgpt-web/high --live-workspace-task` 完成读取 input.txt、写入 result.txt、命令读取校验及最终回答。High 回合 `9656d1565cd7` 的 DOM/wire 答案一致（58 字符），completion fence revision=4 已提交。

未改动用户已有的环境识别、工具终态边界修复；这些既有改动随当前工作区构建保留。
